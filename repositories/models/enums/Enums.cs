namespace TaskAdministratorAPI.models.enums
{
    public static class Enums
    {
        public enum TaskStatus
        {
            Active = 1,
            Select = 0,
            Deleted = -1,
            Completed = 2
        }
    }
}
